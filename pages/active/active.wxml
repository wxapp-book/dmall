<import src="../../views/modules/modules.wxml"/>

<view class="active modules base-style">
	<block wx:for="{{modules}}">
		<template is="{{item.type}}" data="{{...item, index}}"></template>
	</block>		
</view>	